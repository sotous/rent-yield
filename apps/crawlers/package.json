{
  "name": "@rent-yield/crawlers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:fixtures": "vitest run src/application/fixture-directory.test.ts"
  },
  "dependencies": {
    "@rent-yield/listing-storage-contracts": "workspace:*",
    "parse5": "8.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "6.0.2",
    "vitest": "4.1.10"
  }
}
