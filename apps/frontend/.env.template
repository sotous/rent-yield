# Local development uses the same-origin Vite proxy, which forwards to the
# backend on http://127.0.0.1:3001 without triggering browser CORS checks.
VITE_API_BASE_URL=/api

# Production example when the API is hosted separately:
# VITE_API_BASE_URL=https://api.example.com/api
